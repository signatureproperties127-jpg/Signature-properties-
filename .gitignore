node_modules/
package-lock.json

# Runtime database backups and migration manifests are generated at run-time;
# only the named fixtures committed below should be tracked.
data/backups/sig-realty-db-backup-*.json
data/backups/migration-manifest-*.json
